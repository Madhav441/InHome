# Policy engine

- Deterministic TypeScript core for allow/block/warn decisions.
- Bundles signed and versioned; clients verify before applying.
- Schedules expressed in local profile timezone with support for AU daylight saving transitions (TODO).
