# Docker Compose deployment template.
# Copy to infra/docker-compose/.env and uncomment with your values.

# Container images
# API_IMAGE=inhome/mdm-api:latest
# UI_IMAGE=inhome/mdm-ui:latest
# WORKER_IMAGE=inhome/mdm-worker:latest

# Host port overrides
# API_PORT=8080
# UI_PORT=3000

# Location of mounted secrets directory
# SECRETS_DIR=infra/secrets
