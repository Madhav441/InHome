{
  "name": "@sentinel-au/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsup src/background.ts --format esm --out-dir dist",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.262",
    "tsup": "^8.1.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@sentinel-au/sdk-web": "workspace:*"
  }
}
